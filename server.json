{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.nickbasharin/aifra",
  "title": "AIfra — temporary static previews",
  "websiteUrl": "https://aifra.ru/docs/mcp/",
  "description": "48-hour HTTPS previews of prebuilt static sites and HTML slides. No server builds or backends.",
  "version": "0.2.0-beta.1",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "aifra",
      "version": "0.2.0-beta.1",
      "transport": { "type": "stdio" },
      "packageArguments": [{ "type": "positional", "value": "mcp" }],
      "environmentVariables": [
        {
          "name": "AI_DEPLOY_API_ORIGIN",
          "description": "Explicit HTTPS API origin",
          "isRequired": true,
          "format": "string"
        },
        {
          "name": "AIFRA_PROJECT_ROOT",
          "description": "Absolute trusted local project directory; only prebuilt relative subdirectories may be uploaded",
          "isRequired": true,
          "format": "string"
        }
      ]
    }
  ]
}
